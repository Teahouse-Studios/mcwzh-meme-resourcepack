{
    "scripts": {
        "format": "prettier --write ./**/*.{js,json}"
    },
    "dependencies": {
        "glob": "^8.0.3",
        "memepack-builder": "^0.2.0-beta.1"
    },
    "devDependencies": {
        "prettier": "^2.6.2"
    }
}
